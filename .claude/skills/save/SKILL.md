---
name: save
description: บันทึกงานและ context ลง local (ไม่ push GitHub)
user-invocable: true
---

# /save - บันทึก Context (ไม่ Push GitHub)

เมื่อ user พิมพ์ /save ให้บันทึกงานทั้งหมดตามขั้นตอนนี้:

## 1. อัพเดท .claude/rules/

### อัพเดท `09-changelog.md`
เพิ่มสิ่งที่เพิ่งทำไป:
```markdown
## [วันที่ปัจจุบัน] - [หัวข้อสั้นๆ]

### Added
- ...

### Changed
- ...

### Fixed
- ...

### Notes
- สิ่งที่ต้องทำต่อ: ...
```

### อัพเดทไฟล์อื่นๆ (ถ้าจำเป็น)
- `00-prd.md` - ถ้ามี requirements ใหม่
- `01-overview.md` - ถ้าโครงสร้างเปลี่ยน
- ไฟล์อื่นๆ ตาม stack ที่ใช้

---

## 2. อัพเดท CLAUDE.md

อัพเดท index และ Quick Reference ให้ตรงกับสถานะปัจจุบัน:
- Project status
- Stack ที่ใช้
- Quick commands

---

## 3. Commit (ไม่ Push)

### สร้าง commit เท่านั้น
```bash
git add .
git commit -m "Save: [สรุปสั้นๆ สิ่งที่ทำ]"
```

> ⚠️ **ไม่ push ขึ้น GitHub** - ถ้าต้องการ push ด้วย ใช้ `/save-github` แทน

---

## 4. แจ้ง user

```
✅ บันทึกงานเรียบร้อยแล้วครับ! (Local เท่านั้น)

📝 สิ่งที่อัพเดท:
- .claude/rules/09-changelog.md
- [ไฟล์อื่นๆ ที่อัพเดท]

📌 สิ่งที่ทำไป:
- [สรุปสั้นๆ]

🔜 สิ่งที่ต้องทำต่อ:
- [รายการ]

💡 ต้องการ push ขึ้น GitHub ด้วยไหม?
   - ใช้ /save-github (บันทึก + push)
   - ใช้ /github (push อย่างเดียว)
```

---

## หมายเหตุ

- Skill นี้**ไม่** push ขึ้น GitHub
- ถ้ามีไฟล์ sensitive (.env, credentials) → ห้าม commit, เตือน user
- ถ้าไม่มี changes → บอก user ว่า "ไม่มีอะไรต้องบันทึกครับ"
