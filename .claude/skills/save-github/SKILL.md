---
name: save-github
description: บันทึกงานและ push ขึ้น GitHub
user-invocable: true
---

# /save-github - บันทึก Context + Push GitHub

เมื่อ user พิมพ์ /save-github ให้บันทึกงานและ push ขึ้น GitHub:

## 1. อัพเดท .claude/rules/

### อัพเดท `09-changelog.md`
เพิ่มสิ่งที่เพิ่งทำไป:
```markdown
## [วันที่ปัจจุบัน] - [หัวข้อสั้นๆ]

### Added
- ...

### Changed
- ...

### Fixed
- ...

### Notes
- สิ่งที่ต้องทำต่อ: ...
```

### อัพเดทไฟล์อื่นๆ (ถ้าจำเป็น)
- `00-prd.md` - ถ้ามี requirements ใหม่
- `01-overview.md` - ถ้าโครงสร้างเปลี่ยน

---

## 2. อัพเดท CLAUDE.md

อัพเดท index และ Quick Reference ให้ตรงกับสถานะปัจจุบัน

---

## 3. ตรวจสอบ Git Remote

### ถ้ายังไม่มี remote repo
ถาม user:
```
📦 ยังไม่มี GitHub repo สำหรับโปรเจคนี้

ต้องการสร้าง repo ใหม่ไหมครับ?
1. 🔒 Private - เห็นเฉพาะคนที่ได้รับเชิญ
2. 🌍 Public - ทุกคนเห็นได้
3. ❌ ไม่สร้าง - บันทึก local อย่างเดียว
```

### ถ้าเลือกสร้าง repo ใหม่
```bash
# Private
gh repo create [project-name] --private --source=. --push

# Public
gh repo create [project-name] --public --source=. --push
```

---

## 4. Commit & Push

```bash
git add .
git commit -m "Save: [สรุปสั้นๆ สิ่งที่ทำ]"
git push
```

---

## 5. แจ้ง user

```
✅ บันทึกและ Push ขึ้น GitHub เรียบร้อยแล้วครับ!

📝 สิ่งที่อัพเดท:
- .claude/rules/09-changelog.md
- [ไฟล์อื่นๆ]

📌 สิ่งที่ทำไป:
- [สรุปสั้นๆ]

🔗 GitHub: [repo URL]

🔜 สิ่งที่ต้องทำต่อ:
- [รายการ]

💡 เปลี่ยนเครื่องมาทำต่อได้เลยครับ แค่ `git pull` แล้วพิมพ์ `/start`
```

---

## หมายเหตุ

- ตรวจสอบ .gitignore ก่อน commit เสมอ
- ไม่ commit ไฟล์ sensitive (.env, credentials, *.local.md)
- ถ้า push failed → แจ้ง user และแนะนำวิธีแก้
- ถ้าไม่มี changes → บอก user ว่า "ไม่มีอะไรต้องบันทึกครับ"
