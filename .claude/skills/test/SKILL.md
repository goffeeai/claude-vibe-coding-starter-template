---
name: test
description: รัน tests และ auto-fix ถ้ามี errors
user-invocable: true
---

# /test - รัน Tests และ Auto-fix

เมื่อ user พิมพ์ /test ให้ทำตามขั้นตอนนี้:

## 1. ตรวจสอบ Test Framework

ดูจาก package.json หรือไฟล์ config:

| ไฟล์/Package | Framework |
|--------------|-----------|
| `playwright.config.ts` | Playwright (E2E) |
| `jest.config.js` | Jest |
| `vitest.config.ts` | Vitest |
| `cypress.config.js` | Cypress |
| `pytest.ini` / `pyproject.toml` | pytest |

---

## 2. รัน Tests

### ถ้าพบ test framework:
```
🧪 พบ [Framework Name] ในโปรเจค

กำลังรัน tests...
```

รันคำสั่งที่เหมาะสม:
- **Playwright**: `npx playwright test`
- **Jest**: `npm test` หรือ `npx jest`
- **Vitest**: `npx vitest run`
- **Cypress**: `npx cypress run`
- **pytest**: `pytest`

### ถ้าไม่พบ test framework:
```
🧪 ยังไม่มี test framework ในโปรเจค

ต้องการติดตั้งไหมครับ?
1. Playwright (E2E testing) - แนะนำสำหรับ web app
2. Vitest (Unit testing) - เร็ว, เหมาะกับ Vite
3. Jest (Unit testing) - classic, รองรับหลาย framework
4. ยังไม่ต้อง
```

---

## 3. วิเคราะห์ผลลัพธ์

### ถ้า Tests ผ่านทั้งหมด:
```
✅ Tests ผ่านทั้งหมด!

📊 สรุป:
- Total: X tests
- Passed: X
- Failed: 0
- Duration: X.Xs
```

### ถ้ามี Tests ที่ fail:
```
❌ พบ X tests ที่ fail

📋 รายการที่ fail:
1. [test name] - [error message]
2. [test name] - [error message]

🔧 กำลังวิเคราะห์และแก้ไข...
```

---

## 4. Auto-fix Loop

เมื่อพบ tests ที่ fail:

1. **วิเคราะห์ error** - อ่าน error message และ stack trace
2. **หา root cause** - ดูโค้ดที่เกี่ยวข้อง
3. **แก้ไข** - แก้โค้ดที่มีปัญหา
4. **รัน test อีกครั้ง** - ตรวจสอบว่าแก้ได้จริง
5. **วนซ้ำ** - ถ้ายัง fail ให้ทำซ้ำ (สูงสุด 3 รอบ)

```
🔄 รอบที่ 1/3

แก้ไข: [ชื่อไฟล์]
- [สิ่งที่แก้]

กำลังรัน tests อีกครั้ง...
```

---

## 5. สรุปผล

### ถ้าแก้สำเร็จ:
```
✅ แก้ไขเสร็จสิ้น!

📊 สรุป:
- Tests ที่แก้: X
- รอบที่ใช้: X/3

📝 สิ่งที่แก้ไข:
- [ไฟล์] - [สิ่งที่แก้]
```

### ถ้าแก้ไม่สำเร็จ (หลัง 3 รอบ):
```
⚠️ ยังมี tests ที่ fail อยู่

📋 Tests ที่ยังไม่ผ่าน:
1. [test name]

💡 แนะนำ:
- ตรวจสอบ test case ว่าถูกต้องไหม
- อาจต้องแก้ logic ของ feature

ต้องการให้ช่วยวิเคราะห์เพิ่มเติมไหมครับ?
```

---

## หมายเหตุ

- ถ้าไม่มี tests เลย → แนะนำให้เขียน test ก่อน
- ถ้า test framework ยังไม่ setup → ช่วย setup ให้
- ไม่แก้ test case เอง ถ้าไม่แน่ใจว่า test ผิด
- สูงสุด 3 รอบ เพื่อป้องกัน infinite loop
