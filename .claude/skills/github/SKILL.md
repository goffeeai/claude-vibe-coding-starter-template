---
name: github
description: Push งานขึ้น GitHub
user-invocable: true
---

# /github - Push ขึ้น GitHub

เมื่อ user พิมพ์ /github ให้ push งานขึ้น GitHub:

## 1. ตรวจสอบ Git Status

```bash
git status
```

ถ้ามี uncommitted changes:
```
📝 มีไฟล์ที่ยังไม่ได้ commit:
- file1.js
- file2.ts

ต้องการ commit ก่อน push ไหมครับ?
1. ✅ Commit แล้ว push
2. ❌ ยกเลิก
```

---

## 2. ตรวจสอบ Remote Repository

### ถ้ายังไม่มี remote repo
**ต้องถาม user ก่อนเสมอ:**

```
📦 ยังไม่มี GitHub repo สำหรับโปรเจคนี้

ต้องการสร้าง repo ใหม่ไหมครับ?
1. 🔒 Private - เห็นเฉพาะคนที่ได้รับเชิญ (แนะนำสำหรับโปรเจคที่มี credentials/secrets)
2. 🌍 Public - ทุกคนเห็นได้ (แนะนำสำหรับ open source)
3. ❌ ยกเลิก
```

### ถ้าเลือกสร้าง repo ใหม่

**Private:**
```bash
gh repo create [project-name] --private --source=. --push
```

**Public:**
```bash
gh repo create [project-name] --public --source=. --push
```

---

## 3. Push to GitHub

### ถ้ามี remote แล้ว
```bash
git push origin main
```

### ถ้าเป็น branch ใหม่
```bash
git push -u origin [branch-name]
```

---

## 4. แจ้ง user

### Success
```
✅ Push ขึ้น GitHub เรียบร้อยแล้วครับ!

🔗 Repository: https://github.com/[user]/[repo]
🔒 Visibility: Private/Public
📊 Branch: main

💡 เปลี่ยนเครื่องมาทำต่อได้เลย: git clone [url]
```

### Failed
```
❌ Push ไม่สำเร็จ

🔍 สาเหตุ: [error message]

💡 วิธีแก้:
- [แนะนำวิธีแก้ตาม error]
```

---

## ข้อควรระวัง

### ตรวจสอบก่อน Push เสมอ

1. **ไฟล์ Sensitive** - ต้องไม่มีใน commit
   - `.env`, `.env.local`
   - `*credentials*`
   - `*.local.md`
   - API keys, passwords

2. **ขนาดไฟล์** - ไม่เกิน 100MB
   - node_modules/ (ควร gitignore)
   - build files

3. **Private vs Public**
   - โปรเจคมี secrets → **Private**
   - Open source / Template → **Public**
   - ไม่แน่ใจ → **ถาม user**

---

## หมายเหตุ

- **ต้องถาม Private/Public ก่อนสร้าง repo ใหม่เสมอ**
- ถ้าไม่มี gh CLI → แนะนำให้ติดตั้ง: `npm install -g gh`
- ถ้า authentication failed → แนะนำ: `gh auth login`
