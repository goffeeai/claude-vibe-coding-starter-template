---
name: show-creds
description: แสดง credentials ของโปรเจค
user-invocable: true
---

# Show Credentials (/show-creds)

เมื่อ user พิมพ์ /show-creds ให้แสดง credentials ของโปรเจค:

## 1. ตรวจสอบไฟล์ credentials

อ่านไฟล์ `.claude/rules/99-credentials.local.md`

### ถ้ามีไฟล์

แสดงข้อมูลที่เกี่ยวข้อง:

"🔑 **Credentials ของโปรเจคนี้:**

[แสดงเนื้อหาจาก 99-credentials.local.md]

⚠️ หมายเหตุ: ไฟล์นี้ไม่ได้ commit ขึ้น Git"

### ถ้าไม่มีไฟล์

"⚠️ ยังไม่มีไฟล์ credentials ครับ

ต้องการให้สร้างไหมครับ? ผมจะสร้างไฟล์ `.claude/rules/99-credentials.local.md` ให้

ไฟล์นี้จะเก็บข้อมูล:
- URLs (Production, Staging, Dev)
- Server IPs
- Admin login
- Database credentials
- API keys
- Important links

และจะไม่ถูก commit ขึ้น Git (ปลอดภัย)"

---

## 2. ถ้า user ถามเฉพาะเจาะจง

### "SSH เข้า server ยังไง"
แสดงเฉพาะส่วน Servers:
```
ssh [user]@[ip]
```

### "admin login อะไร"
แสดงเฉพาะส่วน Admin Login:
```
URL: ...
Username: ...
Password: ...
```

### "database connect ยังไง"
แสดงเฉพาะส่วน Database:
```
Host: ...
Port: ...
Database: ...
User: ...
Password: ...
```

### "production url อะไร"
แสดงเฉพาะส่วน URLs:
```
Production: https://...
```

---

## 3. สร้างไฟล์ credentials (ถ้า user ต้องการ)

สร้างไฟล์ `.claude/rules/99-credentials.local.md`:

```markdown
# Project Credentials (Local Only)

⚠️ ไฟล์นี้ไม่ commit ขึ้น Git

## 🌐 URLs

| Environment | URL |
|-------------|-----|
| Production | |
| Staging | |
| Development | http://localhost:3000 |

## 🖥️ Servers

| Server | IP | SSH User | หมายเหตุ |
|--------|-----|----------|----------|
| Production | | | |

## 🔐 Admin Login

| ระบบ | URL | Username | Password |
|------|-----|----------|----------|
| Admin | | | |

## 🗄️ Database

| Host | Database | User | Password |
|------|----------|------|----------|
| | | | |

## 🔑 API Keys

| Service | Key |
|---------|-----|
| | |

## 🔗 Important Links

| ชื่อ | Link |
|------|------|
| GitHub | |
| Cloudflare | |
```

---

## หมายเหตุ

- ไฟล์ credentials ต้องอยู่ใน .gitignore เสมอ
- ถ้า user ถาม credentials แต่ไม่มีไฟล์ → เสนอสร้างให้
- ไม่แสดง credentials ในที่สาธารณะ
