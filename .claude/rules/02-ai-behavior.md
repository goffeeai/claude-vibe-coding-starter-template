# AI Behavior Rules

## พฤติกรรมที่ AI ควรทำ

### 1. คุยเรื่องความต้องการก่อนลงมือทำ

> ⚠️ **สำคัญที่สุด**: ต้องเข้าใจเป้าหมายก่อนเสมอ

ทุกครั้งที่เริ่มงาน (โปรเจคใหม่หรือทำต่อ) ต้องถามก่อน:

```
💬 ก่อนเริ่มทำ ขอเข้าใจความต้องการก่อนนะครับ

1. ต้องการทำอะไร? (เป้าหมาย)
2. มีรายละเอียดอะไรเพิ่มเติมไหม?
3. มี deadline หรือข้อจำกัดอะไรไหม?
```

**หลังจากคุยเสร็จ:**
- ถ้างานซับซ้อน (หลายขั้นตอน, แก้หลายไฟล์) → เข้า **Plan Mode** วางแผนก่อน
- ถ้างานง่าย (แก้ไขเล็กน้อย, task เดียว) → ทำได้เลย

**เหตุผล**: เข้าใจเป้าหมายก่อน = ทำงานถูกทิศทาง ไม่ต้องแก้ทีหลัง

---

### 2. ถามก่อนเปิด Browser

> ⚠️ **สำคัญ**: ห้ามเปิด browser อัตโนมัติ

เมื่อต้องการเปิด browser (เช่น preview, localhost, documentation):

```
🌐 ต้องการเปิด browser ไหมครับ?
URL: http://localhost:3000

1. ✅ เปิดเลย
2. ❌ ไม่ต้อง
```

**เหตุผล**: บางโปรเจคอาจไม่ต้องการให้เปิด browser อัตโนมัติ

---

### 3. ถามก่อนสร้าง GitHub Repo

เมื่อต้องการ push ขึ้น GitHub แต่ยังไม่มี repo:

```
📦 ยังไม่มี GitHub repo สำหรับโปรเจคนี้

ต้องการสร้าง repo ใหม่ไหมครับ?
1. 🔒 Private - เห็นเฉพาะคนที่ได้รับเชิญ
2. 🌍 Public - ทุกคนเห็นได้
3. ❌ ไม่สร้าง
```

**เหตุผล**: บางโปรเจคมี secrets ต้องเป็น Private

---

### 4. ถามก่อนลบไฟล์

เมื่อต้องการลบไฟล์หรือโฟลเดอร์:

```
🗑️ ต้องการลบไฟล์เหล่านี้ไหมครับ?
- src/old-component.tsx
- src/unused-util.ts

1. ✅ ลบเลย
2. ❌ ยกเลิก
```

---

### 5. ถามก่อน Install Dependencies ใหม่

เมื่อต้องการ install package ใหม่:

```
📦 ต้องการติดตั้ง package นี้ไหมครับ?
- axios (HTTP client)

1. ✅ ติดตั้งเลย
2. ❌ ไม่ต้อง
```

---

### 6. แจ้งเตือนเรื่อง Sensitive Files

ก่อน commit ต้องตรวจสอบว่าไม่มีไฟล์ sensitive:

```
⚠️ พบไฟล์ที่อาจมีข้อมูลสำคัญ:
- .env.local
- config/secrets.json

ไฟล์เหล่านี้ไม่ควร commit ขึ้น GitHub
ตรวจสอบ .gitignore แล้วหรือยังครับ?
```

---

### 7. แนะนำ UI Framework สำหรับ Frontend

เมื่อสร้างโปรเจค frontend ใหม่ ต้องแนะนำ UI framework:

```
🎨 แนะนำให้ใช้ UI Framework เพื่อ:
- หน้าตาสวยงาม
- รองรับ Mobile (Responsive)
- ประหยัดเวลา

แนะนำ:
1. Tailwind + DaisyUI (ง่าย, เร็ว)
2. Tailwind + Shadcn/ui (สวย, customizable)
3. Bootstrap (classic, เอกสารเยอะ)

เลือกตัวไหนดีครับ?
```

**เหตุผล**: หลายคนลืมใช้ UI framework ทำให้หน้าเว็บไม่สวยและไม่ responsive

---

### 8. แนะนำ Analytics สำหรับเว็บที่ต้องการ SEO/Ads

เมื่อทำโปรเจคประเภท:
- Landing Page / Sale Page
- Content Website / Blog
- Knowledge Base / FAQ / Documentation

**ถามก่อนว่า:** "เว็บนี้จะยิง Ads ด้วยไหมครับ?"

#### ถ้า SEO อย่างเดียว (ไม่ยิง Ads):
```
📊 แนะนำให้ติดตั้ง:

✅ ต้องมี (ฟรี):
1. Google Analytics 4 (GA4) - วัดผู้เข้าชม
2. Google Search Console (GSC) - วัดผล SEO

ดูวิธีติดตั้งที่ .claude/rules/google-ga4-gsc-gtm.md
```

#### ถ้า SEO + Ads (ยิงโฆษณาด้วย):
```
📊 แนะนำให้ติดตั้ง:

✅ พื้นฐาน:
1. Google Analytics 4 (GA4) - วัดผู้เข้าชม
2. Google Search Console (GSC) - วัดผล SEO
3. Google Tag Manager (GTM) - จัดการ Pixels ⭐ สำคัญมาก

📈 ตาม Ads Platform ที่ใช้:
- Google Ads → Google Ads Tag + Conversion Linker
- Facebook/IG Ads → Meta Pixel
- TikTok Ads → TikTok Pixel
- Line Ads → Line Tag

ดูวิธีติดตั้งที่ .claude/rules/google-ga4-gsc-gtm.md
```

**เหตุผล**:
- SEO → ต้องวัดผลเพื่อปรับปรุง content
- Ads → ต้องติด Pixels เพื่อวัด conversion และทำ retargeting

---

### 9. แนะนำให้รัน Tests หลังเขียนโค้ด

เมื่อเขียนโค้ดเสร็จ (feature ใหม่, bug fix, refactor):

```
🧪 เขียนโค้ดเสร็จแล้ว แนะนำให้รัน tests ครับ

ต้องการให้รัน /test ไหมครับ?
1. ✅ รันเลย
2. ❌ ไม่ต้อง
```

**ถ้ามี tests fail:**
- วิเคราะห์และแก้ไขอัตโนมัติ (สูงสุด 3 รอบ)
- แจ้งผลลัพธ์ให้ user ทราบ

**เหตุผล**: ป้องกัน bugs และ regression ก่อน commit/push

---

### 10. เตือนให้ Save เป็นระยะ

เตือน user ให้ `/save` เมื่อเกิดเหตุการณ์เหล่านี้:

**เตือนเมื่อ:**
- ✅ ทำ feature สำคัญเสร็จ 1 อัน
- ✅ แก้ไขไฟล์ไป 5+ ไฟล์
- ✅ แก้ bug ใหญ่สำเร็จ
- ✅ ก่อนเริ่มทำ feature ใหม่
- ✅ ทำงานมานาน (ประมาณ 30-40 รอบขึ้นไป)

```
💾 ทำ [feature/bug fix] เสร็จแล้ว แนะนำให้ /save ไว้ก่อนนะครับ
กัน context reset แล้วลืมสิ่งที่ทำไป

1. ✅ /save เลย
2. ❌ ยังไม่ต้อง
```

**เหตุผล**: ป้องกันการสูญเสียข้อมูลเมื่อ context reset โดยไม่ทันตั้งตัว

---

### 11. แนะนำ /start หลัง Clone Template

เมื่อช่วย user clone หรือติดตั้ง template นี้เสร็จแล้ว:

```
✅ Clone เรียบร้อยแล้วครับ!

🚀 พิมพ์ /start เพื่อเริ่มต้น setup โปรเจคครับ
```

### 12. ตอบสนอง Skill Commands

เมื่อ user พิมพ์คำสั่ง skill (เช่น `/start`, `/save`, `/help-me`):
1. อ่านไฟล์ `.claude/skills/<command>/SKILL.md`
2. ทำตามคำสั่งในไฟล์นั้น

ตัวอย่าง:
- `/start` → อ่าน `.claude/skills/start/SKILL.md`
- `/save` → อ่าน `.claude/skills/save/SKILL.md`

**เหตุผล**: ทำให้ skill ใช้งานได้ทันทีหลัง clone โดยไม่ต้อง reload VS Code

---

### 13. อัพเดทส่วนที่เกี่ยวข้องทั้งหมด

เมื่อเพิ่ม feature, skill, หรือ rule ใหม่ ต้องอัพเดททุกที่ที่เกี่ยวข้อง:

**Checklist:**
- [ ] README.md (ทั้ง TH และ EN sections)
- [ ] docs/ ที่เกี่ยวข้อง
- [ ] /start skill (ถ้าเพิ่ม skill ใหม่)
- [ ] 03-architecture.md (ถ้าเปลี่ยนโครงสร้าง)
- [ ] 04-decisions.md (ถ้าตัดสินใจสำคัญ)
- [ ] 09-changelog.md
- [ ] Badge counts ใน README (Skills, Rules)

**ตัวอย่าง:** เพิ่ม `/test` skill → อัพเดท:
- README skills table (TH + EN)
- docs/03-skills-guide.md
- /start skill tips
- Changelog

**เหตุผล**: ป้องกันเอกสารไม่ sync กัน ทำให้ user หรือ AI สับสน

---

## สรุป

| Action | ต้องถามก่อน |
|--------|------------|
| เริ่มงานใหม่ | ✅ คุยความต้องการก่อนเสมอ |
| งานซับซ้อน | ✅ เข้า Plan Mode |
| เปิด Browser | ✅ ถามทุกครั้ง |
| สร้าง GitHub Repo | ✅ ถาม Private/Public |
| ลบไฟล์ | ✅ ถามยืนยัน |
| Install Package | ✅ ถามยืนยัน |
| Commit Sensitive Files | ⚠️ เตือน + ห้าม |
| สร้าง Frontend ใหม่ | ✅ แนะนำ UI Framework |
| เว็บที่ต้องการ SEO | ✅ แนะนำ Analytics (GA4, GSC) |
| เขียนโค้ดเสร็จ | ✅ แนะนำรัน /test |
| ทำงานมาพอสมควร | ✅ เตือนให้ /save |
| Clone template เสร็จ | ✅ แนะนำ /start |
| User พิมพ์ skill command | ✅ อ่าน SKILL.md แล้วทำตาม |
| เพิ่ม feature/skill ใหม่ | ✅ อัพเดททุกที่ที่เกี่ยวข้อง |
| งานง่ายๆ | ❌ ทำได้เลย |
| Run Dev Server | ❌ ไม่ต้องถาม |
| Read Files | ❌ ไม่ต้องถาม |
| Edit Files | ❌ ไม่ต้องถาม |
