<!--
AI INSTRUCTION:
1. ก่อน clone template นี้ → ตรวจสอบว่าโฟลเดอร์ปัจจุบันว่างหรือไม่
   - ถ้าว่าง → clone ได้เลย ใช้ `git clone <url> .` (มีจุดต่อท้าย)
   - ถ้าไม่ว่าง (มีไฟล์/โปรเจคอยู่แล้ว) → ถาม user ว่าต้องการ copy แค่ .claude/ และ docs/ แทนไหม
2. เมื่อช่วย user clone/copy template เสร็จ → แนะนำให้พิมพ์ /start
3. เมื่อ user พิมพ์ /start, /save, /save-github, /github, /help-me, /show-creds, /test, /seo, /find-skills
   → อ่านไฟล์ .claude/skills/<command>/SKILL.md แล้วทำตามคำสั่งในนั้น
   ตัวอย่าง: /start → อ่าน .claude/skills/start/SKILL.md
-->

# Project Name

> 💡 เปลี่ยนชื่อโปรเจคด้านบน และลบบรรทัดนี้ออก
>
> 🚀 **เริ่มต้นใช้งาน:** พิมพ์ `/start` เพื่อ setup โปรเจค

## 💾 Save Context

เมื่อ context ใกล้เต็ม หรือทำงานเสร็จ 1 task ใหญ่:
→ พิมพ์ `/save` (บันทึก local)
→ พิมพ์ `/save-github` (บันทึก + push)

---

## 📁 Project Info

| Key | Value |
|-----|-------|
| **Stack** | (ใส่ stack ที่ใช้) |
| **Database** | (ใส่ database ที่ใช้) |
| **Status** | 🚧 In Progress |

---

## 🎮 Quick Commands

| Command | Description |
|---------|-------------|
| `/start` | 🚀 เริ่มใช้งาน / setup โปรเจค |
| `/save` | 💾 บันทึกงาน (local) |
| `/save-github` | 📤 บันทึก + push GitHub |
| `/github` | 🔗 Push ขึ้น GitHub |
| `/help-me` | 🆘 ขอความช่วยเหลือ |
| `/show-creds` | 🔑 ดู credentials |

---

## 📖 Documentation

รายละเอียดเพิ่มเติมอยู่ใน `.claude/rules/`:

| ไฟล์ | เนื้อหา |
|------|--------|
| `00-prd.md` | Product Requirements |
| `01-overview.md` | Project overview |
| `02-ai-behavior.md` | AI behavior rules |
| `09-changelog.md` | Recent changes |

---

## 🚀 Quick Start

```bash
# Development
npm install
npm run dev

# Production
npm run build
npm start
```

---

> ℹ️ ไฟล์นี้จะถูกอัพเดทอัตโนมัติเมื่อใช้ `/save` หรือ `/save-github`
